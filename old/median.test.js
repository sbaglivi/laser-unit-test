
function median(numbers) {
    return
}

// test("median of non array to throw type error", () => {
//     expect(() => median("a")).toThrow(TypeError)
// })

// test("median of single value (1) to return value itself", () => {
//     expect(median([1])).toEqual(1);
// })

// test("median of even number of values to work (1,3) -> 2", () => {
//     expect(median([1,3])).toEqual(2);
// })

// test("median of odd number of values to work (1,3,5) -> 3", () => {
//     expect(median([1,3,5])).toEqual(3);
// })

// test("median of negative values to work (-1,-3) -> -2", () => {
//     expect(median([-1,-3])).toEqual(-2);
// })

// test("median of empty array to throw error", () => {
//     expect(() => median([])).toThrow(RangeError)
// })

// test("median of floating point", () => {
//     expect(median([1.0, 3.5])).toBeCloseTo(2.25)
// })


